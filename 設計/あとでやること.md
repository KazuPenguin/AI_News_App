# あとでやることリスト

開発フェーズと本番運用フェーズで実施すべきタスクを整理しました。

---

## 🚀 今すぐやる (開発フェーズで必要なタスク)

アプリケーション本体の開発を進めるために、**今すぐ着手・解決すべき**項目です。

### Secrets Manager (Batch/Auth 開発用)
開発に必要な外部APIキーや認証情報を登録します。
**パイプラインを動かすには最低限 OpenAI と Gemini のキーが必要です。**
- [ ] **OpenAI API Key** を Secrets Manager に登録
  - `aws secretsmanager create-secret --name ai-research/openai-api-key --secret-string '{"api_key":"sk-xxx"}'`
  - 用途: L2 Embedding 生成 (`text-embedding-3-small`)
- [ ] **Google AI Studio Key** を Secrets Manager に登録

  - `aws secretsmanager create-secret --name ai-research/gemini-api-key --secret-string '{"api_key":"xxx"}'`
  - 用途: L3 分析 + Post-L3 PDF全文分析 (`gemini-2.0-flash`)
- [ ] **Google OAuth クライアント ID / Secret** (未取得なら取得) → Secrets Manager に登録
- [ ] **Apple Sign In の Service ID / Key** (未取得なら取得) → Secrets Manager に登録

### DB マイグレーション (パイプライン用テーブル)
パイプラインが使用するテーブルが DB に存在する必要があります。
**Alembic マイグレーションスクリプト作成済み** (`alembic/versions/20260219_001_initial_schema.py`)
- [ ] **Alembic マイグレーション実行** — `cd backend && uv run alembic upgrade head` で全テーブル＋pgvector拡張を一括作成
- [ ] **アンカーデータ投入** — `uv run python scripts/setup_anchors.py` でアンカーベクトルを初期投入
  - 事前に環境変数 `DATABASE_URL` と `OPENAI_API_KEY` を設定

### バッチ Lambda のデプロイ・動作確認
**パイプラインコード実装済み:** `batch/handler.py`, `batch/pipeline.py`, `batch/l1_collector.py`, `batch/l2_selector.py`, `batch/l3_analyzer.py`, `batch/post_l3_reviewer.py`, `Dockerfile.batch`
- [ ] **CDK デプロイ** — `cd infra && npx cdk deploy BatchStack` で Docker イメージを ECR にプッシュ & Lambda 更新
- [ ] **手動実行テスト** — `aws lambda invoke --function-name <BatchFunctionName> /dev/stdout` で動作確認
- [ ] **CloudWatch Logs 確認** — パイプライン各フェーズのログが正しく出力されることを確認
- [ ] **batch_logs テーブル確認** — 実行後に `SELECT * FROM batch_logs ORDER BY execution_date DESC LIMIT 1;` で記録確認

### パイプライン閾値チューニング (運用開始後1週間)
初回動作後、実際のデータでフィルタ強度を調整します。
- [ ] **L2 閾値調整** — `batch/config.py` の `L2_THRESHOLD` (初期値: 0.40)
  - 通過率 > 50% → 0.45 に引き上げ
  - 通過率 < 20% → 0.35 に引き下げ
  - 目標通過率: 30-50%
- [ ] **L3 結果の精度確認** — `is_relevant=true` の論文が本当に有用か目視チェック
- [ ] **Post-L3 出力品質確認** — `detail_review` JSON の日本語品質・セクション選択の妥当性を確認

### API エンドポイントの実装 (完了 2026/2/20)
FastAPI + Mangum で全11エンドポイント実装済み。テスト17件パス。
- [x] **論文一覧 API** — `GET /papers` (カーソルページネーション, category_id/importance/date フィルタ)
- [x] **論文詳細 API** — `GET /papers/:arxiv_id` (detail_review JSONB → セクション/3視点/レベル別パース)
- [x] **論文図表 API** — `GET /papers/:arxiv_id/figures`
- [x] **閲覧記録 API** — `POST /papers/:arxiv_id/view` (UPSERT)
- [x] **ブックマーク API** — `GET/POST/DELETE /bookmarks` (409重複/403権限チェック)
- [x] **ユーザー API** — `GET /users/me`, `PUT /users/me/settings`, `GET /users/me/stats`
- [x] **カテゴリ API** — `GET /categories` (論文数集計)
- [x] **ヘルスチェック API** — `GET /health`

### Frontend-Backend 結合 (完了 2026/2/20)
全画面をモックデータから実 API 呼び出しに書き換え済み。
- [x] **API クライアント層** — `@tanstack/react-query`, `lib/api-client.ts` (Cognito token), `lib/api.ts` (型定義+関数)
- [x] **React Query hooks** — `usePapers`, `useBookmarks`, `useUser` (useInfiniteQuery/useMutation)
- [x] **ホーム画面** — カテゴリタブ + 論文一覧 (無限スクロール)
- [x] **論文詳細画面** — 実データ表示 + 閲覧記録自動送信 + ブックマーク操作
- [x] **お気に入り画面** — ブックマーク一覧 (無限スクロール)
- [x] **設定画面** — ユーザープロフィール + 統計 + 難易度変更

### API Lambda のデプロイ・動作確認
- [ ] **CDK デプロイ** — `cd infra && npx cdk deploy ApiStack` で API Lambda 更新
- [ ] **Frontend `.env` の API URL 更新** — デプロイ後の実際の API Gateway URL に置換
- [ ] **結合テスト** — ローカル `uvicorn api.app:app` または API Gateway URL で全エンドポイント疎通確認

---

## ⏳ 後でやる (本番運用・リリース前のタスク)

機能開発が完了し、本番環境への移行やストア公開の準備段階で実施する項目です。

### 本番環境への移行時（CDK 設定変更）
- [ ] **`database-stack.ts`**: `removalPolicy` を `cdk.RemovalPolicy.RETAIN` に変更（DB 削除防止）
- [ ] **`database-stack.ts`**: `multiAz` を `true` に変更（高可用性）
- [ ] **`database-stack.ts`**: `deletionProtection` を `true` に変更
- [ ] **`database-stack.ts`**: インスタンスタイプを `db.t4g.small` 以上に変更
- [ ] **`network-stack.ts`**: `natGateways` を `2` に変更（AZ 冗長化）
- [ ] **`api-stack.ts`**: CORS `allowOrigins` を `ALL_ORIGINS` から本番ドメインに制限
- [ ] **`api-stack.ts`**: `dataTraceEnabled` が `false` であることを確認（リクエストボディのログ無効化）
- [ ] **`auth-stack.ts`**: メール送信を Cognito デフォルトから **SES** に切り替え（送信制限の緩和）
- [ ] **`auth-stack.ts`**: `callbackUrls` / `logoutUrls` を正式なカスタムスキームに更新（`myapp://` → 正式値）

### ネットワーク・セキュリティ強化
- [ ] **VPC Endpoints の追加検討** — NAT Gateway 経由のコスト削減
- [ ] **CloudFront にカスタムドメイン設定** — `cdn.example.com` 等（ACM 証明書の取得含む）
- [ ] **API Gateway にカスタムドメイン設定** — `api.example.com`（Route 53 + ACM 証明書）
- [ ] **WAF の追加検討** — CloudFront / API Gateway への Web ACL 設定

### AuthStack (ソーシャルログイン有効化)
Secrets Manager に登録したキーを使って CDK の設定を有効化します。
- [ ] **Google ソーシャルログインの有効化** — `auth-stack.ts` のコメントアウト解除
- [ ] **Apple ソーシャルログインの有効化** — `auth-stack.ts` のコメントアウト解除

### CI/CD (Phase 2 & 3: デプロイ・運用)
- [ ] **Frontend (Web)**: `infra` に Hosting 用スタック (S3 + CloudFront) を追加
- [ ] **Frontend (Web)**: GitHub Actions に Build & Deploy ジョブを追加
- [ ] **Frontend (Mobile)**: EAS (Expo Application Services) プロジェクトの初期化と CI 連携
- [ ] **E2E テスト**: Playwright 等を用いた統合テストの導入
- [ ] **監視/アラート**: 異常検知時の通知設定

### バッチパイプライン運用
- [x] ~~**Lambda パッケージングの検討** — DockerImageFunction への移行検討~~ → **済: Dockerfile.batch で実装済み**
- [ ] **DLQ (Dead Letter Queue) の追加** — バッチ失敗時の通知・リトライ用
- [ ] **API キーの追加検討** — Usage Plan でクライアントアプリ単位のスロットリング
- [ ] **CDN_DOMAIN 環境変数の設定** — `batch-stack.ts` に CloudFront ドメインを追加（図表の公開URL生成に使用）
- [ ] **Gemini モデルバージョンの更新** — `batch/config.py` の `GEMINI_MODEL` を新バージョン公開時に更新

---

## ✅ デプロイ後の検証（最終確認）
- [ ] pgvector 拡張の有効化確認 (`SELECT * FROM pg_extension;`)
- [ ] EventBridge ルールの手動テスト実行
- [ ] Cognito テストユーザーでログインフロー確認
- [ ] API エンドポイントの疎通確認（`/health` で 200 応答）
- [ ] API 全エンドポイントの疎通確認（`/papers`, `/categories`, `/bookmarks`, `/users/me` 等）
- [ ] Cognito Authorizer 経由で認証付きリクエストが通ることの確認
- [ ] バッチ Lambda の手動実行テスト（`aws lambda invoke`）
- [ ] batch_logs テーブルにログが記録されていることの確認
- [ ] papers テーブルに L1/L2/L3 の結果が正しく書き込まれていることの確認
- [ ] paper_figures テーブルに図表メタデータが挿入されていることの確認
- [ ] S3 `figures/` プレフィックスに画像ファイルがアップロードされていることの確認
- [ ] Frontend アプリからの API 呼び出しが正常に動作することの確認（ホーム/詳細/お気に入り/設定）
